apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "critik8s-emit.fullname" . }}-test-connection"
  labels:
    {{- include "critik8s-emit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "critik8s-emit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
